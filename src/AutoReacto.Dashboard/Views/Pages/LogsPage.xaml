<Page x:Class="AutoReacto.Dashboard.Views.Pages.LogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:AutoReacto.Dashboard.Localization"
      Title="Logs"
      DataContext="{x:Static local:LocalizationManager.Instance}">

    <DockPanel>
        <!-- Header -->
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,16">
            <Button Content="{Binding Strings.ClearLogs}" 
                    DockPanel.Dock="Right"
                    Style="{StaticResource DiscordDangerButtonStyle}"
                    Padding="12,8"
                    Click="ClearLogs_Click"/>
            <StackPanel>
                <TextBlock Text="{Binding Strings.BotLogs}" Style="{StaticResource DiscordTitleStyle}"/>
                <TextBlock Text="{Binding Strings.RealTimeLogs}" 
                           Foreground="{StaticResource DiscordTextMutedBrush}" 
                           FontSize="12"/>
            </StackPanel>
        </DockPanel>

        <!-- Log Display -->
        <Border Background="{StaticResource DiscordDarkerBrush}" CornerRadius="8" Padding="12">
            <ScrollViewer x:Name="LogScrollViewer" 
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Auto">
                <TextBlock x:Name="LogTextBlock" 
                           FontFamily="Consolas"
                           FontSize="12"
                           Foreground="{StaticResource DiscordTextBrush}"
                           TextWrapping="NoWrap"/>
            </ScrollViewer>
        </Border>
    </DockPanel>
</Page>
